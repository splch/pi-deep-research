{
  "name": "pi-deep-research",
  "version": "0.3.0",
  "description": "Multi-agent deep research extension for pi: planner → parallel workers → writer → CitationAgent → URL verify, with confidence labels, dead-link markers, structured briefs, domain presets, per-phase model cascading, an optional cost cap with graceful partial-results, and a full provenance manifest.",
  "keywords": ["pi-package", "deep-research", "agents", "research", "llm"],
  "license": "MIT",
  "type": "module",
  "private": false,
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "check": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs",
    "test": "npm run check && npm run smoke"
  },
  "peerDependencies": {
    "@mariozechner/pi-ai": "*",
    "@mariozechner/pi-coding-agent": "*",
    "@mariozechner/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
